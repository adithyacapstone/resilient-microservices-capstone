apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: recovery-engine-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: recovery-engine
  endpoints:
    - port: metrics
      path: /actuator/prometheus
      interval: 15s
