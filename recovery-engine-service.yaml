apiVersion: v1
kind: Service
metadata:
  name: recovery-engine
  namespace: default
  labels:
    app: recovery-engine
    monitoring: enabled
spec:
  type: NodePort
  selector:
    app: recovery-engine
  ports:
    - name: metrics
      port: 8090
      targetPort: 8090
      nodePort: 30090
